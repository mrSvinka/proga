Code review
6. 
Выход за пределы массива (mas[5] = 10 обращается к шестому элементу массива, а их всего 5)
Неправильное освобождение памяти(надо написать delete[], т.к. создавали массив).
7.
Функция возвращает адрес локальной переменной tmp. После завершения функции эта переменная выходит из области видимости, что приведет к неопределенному поведению.
8.
Нет ошибок.
9.
В функции обращаться к строке по ссылке, чтобы улучшить эффективность кода.
10.
При вводе строки будет учитываться только то, что введено до первого пробела, ошибок нет.

если чет неправильно напишите